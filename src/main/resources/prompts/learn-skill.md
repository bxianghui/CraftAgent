根据以下对话历史，判断是否有值得提炼为可复用 skill 的解决流程。

判断标准：
- 解决了一个需要多步骤的具体问题
- 该问题在其他 session 中可能再次出现
- 解决方法有明确的步骤和验证方式

如果有，输出以下 JSON（只输出 JSON，不要解释）：
{"name":"短横线连接的英文名称","description":"一句话描述：解决什么问题","content":"完整的 skill 正文（Markdown 格式，包含问题原因/解决步骤/验证方法）"}

如果没有值得提炼的内容，输出空字符串：""

对话历史：
